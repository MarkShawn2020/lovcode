import { AtaruSearchPage } from "@/modules/ui/AtaruSearchPage";

/** Ataru opens directly into its quiet, Google-like recall home. */
export default function HomePage() {
  return <AtaruSearchPage />;
}
